name: Update apple/container CLI

on:
  schedule:
    - cron: '0 8 * * 3'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Get versions
        id: versions
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CURRENT=$(grep 'version ?' pkgs/package.nix | sed 's/.*"\(.*\)".*/\1/')
          LATEST=$(gh release view --repo apple/container --json tagName -q .tagName)
          echo "current=$CURRENT" >> $GITHUB_OUTPUT
          echo "latest=$LATEST" >> $GITHUB_OUTPUT

      - name: Already up to date
        if: steps.versions.outputs.current == steps.versions.outputs.latest
        run: echo "apple/container is already up to date (${{ steps.versions.outputs.current }})"

      - name: Update pkgs/package.nix
        if: steps.versions.outputs.current != steps.versions.outputs.latest
        run: |
          LATEST="${{ steps.versions.outputs.latest }}"
          URL="https://github.com/apple/container/releases/download/${LATEST}/container-${LATEST}-installer-signed.pkg"
          SHA256_HEX=$(curl -sL "$URL" | sha256sum | cut -d' ' -f1)
          SRI="sha256-$(echo "$SHA256_HEX" | xxd -r -p | base64 -w0)"
          sed -i "s|version ? \".*\"|version ? \"${LATEST}\"|" pkgs/package.nix
          sed -i "s|hash ? \".*\"|hash ? \"${SRI}\"|" pkgs/package.nix
          echo "Updated pkgs/package.nix to $LATEST (hash: $SRI)"

      - name: Update .github/badges/versions.json
        if: steps.versions.outputs.current != steps.versions.outputs.latest
        run: |
          jq --arg v "${{ steps.versions.outputs.latest }}" '.apple_container = $v' .github/badges/versions.json > .github/badges/versions.json.tmp
          mv .github/badges/versions.json.tmp .github/badges/versions.json

      - name: Commit and push
        if: steps.versions.outputs.current != steps.versions.outputs.latest
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add pkgs/package.nix .github/badges/versions.json
          git commit -m "chore: bump apple/container to ${{ steps.versions.outputs.latest }}"
          git push origin master
